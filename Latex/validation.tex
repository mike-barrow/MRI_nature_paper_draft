%%==========================================================================%%
%% validation.tex -- "Technical Validation".                                 %%
%% Inter-reader agreement and a trained baseline are both candidate          %%
%% validation evidence, TBD which to publish. American spelling.             %%
%%==========================================================================%%

\section*{Technical Validation}\label{sec:validation}

\subsection*{Inter-reader agreement}\label{subsec:agreement}
% TBD whether inter-reader agreement, a trained baseline, or both serve as the
% primary validation evidence. This subsection is provisional.
% [TBD: include this analysis or omit it in favor of the baseline below.]
For a subset of examinations annotated independently by more than one reader, inter-reader agreement quantifies label reliability, using [TBD: Dice for segmentation overlap and a weighted kappa for categorical labels], broken down by structure where appropriate.

\subsection*{Metadata and data integrity}\label{subsec:integrity}
We verified the integrity and internal consistency of the release through schema validation of the metadata table, checks of referential consistency between the metadata and the imaging and label files, and per-file checksums (SHA-256).
The data additionally pass the curation and validation performed by the host repository as part of submission.

\subsection*{Baseline analysis}\label{subsec:baseline}
% Baseline (optional). Candidate approaches:
%  (a) trained anatomy-segmentation baseline (nnU-Net / TotalSegmentator-based),
%  (b) trained lesion-segmentation baseline (AutoPET-style nnU-Net).
%  Metrics: Dice, HD95, NSD, with bootstrapped 95% CIs.
%  Status: no completed trained model yet.
[TBD: a trained segmentation baseline (anatomy and/or lesion) demonstrating the data are fit for the intended whole-body segmentation tasks, reported with Dice, HD95, and NSD and 95\% confidence intervals.]
