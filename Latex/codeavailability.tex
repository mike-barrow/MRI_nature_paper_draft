%%==========================================================================%%
%% codeavailability.tex -- "Code availability" section.                      %%
%% CONCEPTUAL STUBS ONLY.                                                     %%
%%==========================================================================%%

\section*{Code availability}\label{sec:code}

% List the custom code released with the dataset, its repository, and license:
%   - DICOM -> NIfTI conversion / de-identification pipeline
%   - radiology report scrubbing (PHI removal) pipeline
%   - index builder (plain-text tables -> SQLite)
%   - query/preview client
%   - metadata/integrity validation scripts
%   - any baseline-analysis code
% If a component is not released, state so explicitly.
A note listing the code released alongside the dataset --- the conversion and
de-identification pipeline, the report-scrubbing pipeline, the index builder, the
query/preview client, and the validation scripts --- together with the
repository location and license.
